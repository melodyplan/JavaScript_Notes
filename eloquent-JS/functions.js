var min = function(x, y) {
	if (x > y)
      return y;
    else (y > x)
      return x;
}

console.log(min(0, 10));
// → 0
console.log(min(0, -10));
// → -10

//I tried this and I was shocked it worked the first time and I got it right away! =D